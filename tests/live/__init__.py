# -*- coding: utf-8 -*-
"""Пробы, которые зовут настоящий `claude`.

Они стоят денег и ходят в сеть, поэтому идут только по явной просьбе:
`CCKIT_LIVE=1`. Значение снимается один раз, при импорте пакета: менять
переменную посреди прогона всё равно нельзя — @skipUnless вычисляется на
импорте модуля. Что выключено по умолчанию, утверждает test_live_guard, а не
эта строка.
"""

import os

LIVE = os.environ.get("CCKIT_LIVE") == "1"


def live_reason():
    """Почему проба пропущена — одной строкой, чтобы это было видно в выводе."""
    return "живые прогоны стоят денег: CCKIT_LIVE=1"
